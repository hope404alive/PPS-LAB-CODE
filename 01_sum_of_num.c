#include <stdio.h>
#include <conio.h>
int main()
{
    int a, b, sum;
    printf("enter the two numbers to add \n");
    scanf("%d %d", &a, &b);
    sum = a + b;
    printf("the sum of the two num %d and %d is %d \n", a, b, sum);
    return 0;
}
